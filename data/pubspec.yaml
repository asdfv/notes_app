name: data
description: A new Flutter package project.
version: 0.0.1
author: asdfv

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  cloud_firestore: ^0.16.0+1
  firebase_core: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

