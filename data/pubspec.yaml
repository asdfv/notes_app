name: data
description: Data layer
version: 0.0.1
author: asdfv

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  domain:
    path: ../domain
  cloud_firestore: ^0.16.0+1
  firebase_core: ^0.7.0
